from typing import Protocol, Sequence, Any
from uuid import UUID


class VehicleRepository(Protocol):
    async def create(self, **data) -> Any: ...
    async def get_by_id(self, vehicle_id: UUID) -> Any | None: ...
    async def list(self, limit: int = 50, offset: int = 0) -> Sequence[Any]: ...
    async def update(self, vehicle_id: UUID, **data) -> Any | None: ...
    async def delete(self, vehicle_id: UUID) -> None: ...

